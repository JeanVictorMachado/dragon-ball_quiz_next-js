import { Story, Meta } from '@storybook/react/types-6-0'
import QuizBackground from '.'

export default {
  title: 'QuizBackground',
  component: QuizBackground
} as Meta

export const Basic: Story = (args) => <QuizBackground />
